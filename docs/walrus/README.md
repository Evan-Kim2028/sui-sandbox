# Walrus Integration (Workspace Notes)

This folder centralizes Walrus-related documentation, benchmarks, and proof-of-concept notes for `sui-sandbox`.

Primary entry point example:

- `cargo run --release --example walrus_checkpoint_replay`

