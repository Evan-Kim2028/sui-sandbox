# Sui Sandbox - Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit .env files with real API keys!

# =============================================================================
# Sui gRPC API (Required for transaction replay with historical data)
# =============================================================================
# A gRPC endpoint with historical data access is required for accurate
# transaction replay. This includes unchanged_loaded_runtime_objects
# (dynamic field child versions at their input versions).
#
# Options:
#   - Sui mainnet fullnode (limited historical data): https://fullnode.mainnet.sui.io:443
#   - Third-party providers with enhanced historical access
#
SUI_GRPC_ENDPOINT=https://fullnode.mainnet.sui.io:443
SUI_GRPC_API_KEY=
SUI_GRAPHQL_ENDPOINT=https://graphql.mainnet.sui.io/graphql

# =============================================================================
# Walrus Checkpoint Fetching (Optional)
# =============================================================================
# Enable checkpoint hydration from Walrus (HTTP aggregator + caching server).
# This is useful for high-fidelity checkpoint replay when gRPC/GraphQL are missing
# historical object bytes.
#
# SUI_WALRUS_ENABLED=true
# SUI_WALRUS_NETWORK=mainnet  # or testnet
# SUI_WALRUS_CACHE_URL=https://walrus-sui-archival.mainnet.walrus.space
# SUI_WALRUS_AGGREGATOR_URL=https://aggregator.walrus-mainnet.walrus.space
# SUI_WALRUS_TIMEOUT_SECS=10
#
# Optional local filesystem store for Walrus objects (fast offline replay)
# SUI_WALRUS_LOCAL_STORE=1
# SUI_WALRUS_STORE_DIR=/path/to/walrus-store  # optional override
# SUI_WALRUS_FULL_CHECKPOINT_INGEST=1         # ingest all objects in checkpoint
# SUI_WALRUS_RECURSIVE_LOOKUP=1               # use local index to hydrate missing objects
# SUI_WALRUS_RECURSIVE_MAX_CHECKPOINTS=5      # max checkpoints to pull per replay

# =============================================================================
# Replay Timing (Optional)
# =============================================================================
# Enable detailed timing logs for replay fetch stages.
# SUI_DEBUG_TIMING=1

# Optional: Override the default sandbox home directory (cache, projects, logs)
# Defaults to ~/.sui-sandbox
# SUI_SANDBOX_HOME=/path/to/sui-sandbox

# =============================================================================
# OpenRouter API (For LLM-based features)
# =============================================================================
# OpenRouter provides a single API key for 150+ models from different providers.
# Get your API key at: https://openrouter.ai/
#
# Popular models:
#   - anthropic/claude-sonnet-4
#   - anthropic/claude-opus-4
#   - openai/gpt-4o
#   - openai/gpt-4o-mini
#   - deepseek/deepseek-v3
#   - google/gemini-2.0-flash-exp
#
OPENROUTER_API_KEY=
SMI_API_BASE_URL=https://openrouter.ai/api/v1
SMI_MODEL=anthropic/claude-sonnet-4
SMI_TEMPERATURE=0
SMI_MAX_TOKENS=2048

# Optional: Enable JSON mode for structured output
# SMI_RESPONSE_FORMAT=json_object

# =============================================================================
# Alternative: Direct Provider APIs (OpenAI, Anthropic, etc.)
# =============================================================================
# If using a provider directly instead of OpenRouter:
#
# SMI_API_KEY=your-api-key
# SMI_API_BASE_URL=https://api.openai.com/v1
# SMI_MODEL=gpt-4o

# =============================================================================
# Simulation Settings
# =============================================================================
# Public address for dry-run simulation.
# This address should own at least one Coin<SUI> on mainnet.
# Only the PUBLIC address is needed - no private key required for dry-run.
#
SMI_SENDER=

# Optional: Pin a specific gas coin object ID (otherwise auto-selected)
# SMI_GAS_COIN=

# =============================================================================
# Private Key (ONLY for signing real transactions - NOT needed for simulation)
# =============================================================================
# WARNING: Only set this if you need to sign actual transactions.
# The sandbox and dry-run features do NOT require a private key.
#
# SMI_PRIVATE_KEY=suiprivkey1...
